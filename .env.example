# Copiar a .env y rellenar con credenciales propias:
#   cp .env.example .env
#
# docker compose lee este fichero automaticamente al levantar el stack.
# El valor de DB_PASSWORD debe coincidir con `db_password` de config/odoo.conf.
DB_USER=odoo
DB_PASSWORD=CAMBIAME
